package az.turing.newapp.mapper;

import az.turing.newapp.entity.Message;
import az.turing.newapp.model.request.MessageDTO;

public interface MessageMapper extends EntityMapper<Message, MessageDTO> {
}
